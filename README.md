# Codex Refit

A local starship-console maintenance dashboard and macOS menu bar app for large Codex state folders. It opens in Easy Mode with one `Smart Optimize` button and a `Speed Check` benchmark. Hard Mode reveals the manual controls.

Run it in the browser with:

```sh
npm run dev
```

Build and run it as a local macOS app with:

```sh
npm run package:mac
open "release/Codex Refit.app"
```

The app scans local Codex paths, shows the biggest session, media, log, cache, image, worktree, memory, and Chronicle buckets, and keeps risky actions guarded. `Speed Check` records a stable local readiness score using scan time, state DB latency, log DB latency, WAL size, active transcript size, session media/appshot pressure, task-clarity markers, current-project planning readiness, Goal Mode readiness, Validation Loop readiness, Review Lane readiness, Codex manual freshness, observed turn telemetry, approval/sandbox friction markers, permissions-profile shape, Chronicle screen-cache/generated-memory load, stale threads, oversized transcript counts, and Codex-managed worktree pressure; it also records a live timing score that accounts for current Codex process load, background terminal commands, app-server transport health, Codex launch latency, SSH remote-host readiness, default model/reasoning task fit, cloud-handoff readiness, model-provider auth health, named speed-profile health, subagent fan-out settings, custom-agent load, MCP startup pressure, shell startup latency, CLI completion setup, strict config validation, shell environment breadth, notification-flow health, history-retention health, storage-path health, telemetry-export health, Codex exec automation hygiene, context budget and status-line settings, response verbosity/reasoning-summary shape, effective web-search mode, deprecated web-search toggles, network/sandbox friction, permissions profiles, instruction-stack size, custom instruction overrides, command-rule pressure, lifecycle hook load, automatic approval review, memory-injection weight, deprecated custom prompts, skill-catalog metadata pressure, project local-environment setup/action readiness, and file-based managed Codex config conflicts. Speed Proof history compares latest, best, trend, active-folder change, media-session change, task-clarity change, planning-template change, Goal Mode change, Validation Loop change, Review Lane change, Codex manual freshness change, turn-telemetry change, approval-friction change, permissions-profile change, Chronicle-load change, notification-flow change, history-retention change, storage-path change, telemetry-export change, status-line visibility change, CLI-completion change, strict-config change, Codex-launch change, automation change, app-server transport change, remote-host readiness change, shell-startup timing change, cloud-handoff change, live process-count change, worktree-pressure change, model-provider change, model-effort change, speed-profile health change, custom-agent change, response-shape change, effective web-search change, network-policy change, instruction-stack change, instruction-override change, command-rule change, custom-prompt change, skill-catalog changes, local-environment setup/action changes, and managed-config changes after cleanup. Hard Mode shows the score drivers, while Smart Optimize shows a conservative projected score lift before you run it. `Codex Doctor` adds a ranked Next Moves queue plus docs-informed guidance for local state, media-heavy active sessions, task clarity in active threads, Goal Mode readiness, Validation Loop readiness, Review Lane readiness, Codex manual freshness, plan-mode/template readiness, turn timing and rate-limit signals, approval/sandbox stops, scoped permissions profiles, Chronicle screen-memory load, notification settings for long/background work, history retention, storage paths, telemetry export, status-line visibility, CLI completion, Codex launch latency, strict config validation, Codex exec automation, app-server transport, SSH remote hosts for offloading project work, cloud handoff for heavy/parallel work, shell startup latency, live Codex process load, background terminal work, Codex-managed worktrees, model provider/auth setup, model/effort task fit, subagent fan-out, custom agents, MCP startup, shell environment policy, network sandbox, context/tool-output budget, response shape, profile health, instruction stack, instruction overrides, command rules, managed requirements/defaults, lifecycle hooks, approval review, memory context, skill catalog size, auth cache health, model/reasoning settings, Fast Mode, workflow context, trusted project playbooks, local setup actions, and task-size profiles such as Fast Lane, Small Tasks, and Deep Work. You can also run the official `codex doctor --json` check on demand; Refit summarizes the redacted report and offers safe copyable action snippets instead of slowing every scan. In Hard Mode, Doctor also shows durable config advice for shell snapshots, shell startup latency, CLI completion, Codex launch latency, Codex manual freshness, Validation Loop readiness, Review Lane readiness, strict config validation, shell environment policy, notification flow, history retention, storage paths, telemetry export, status-line visibility, Codex exec automation, app-server transport, SSH remote-host config, network access/proxy scope, effective web-search mode, legacy web-search keys, session media pressure, task-clarity pressure, Goal Mode readiness, planning-template scope, turn-telemetry pressure, approval-friction pressure, permissions-profile scope, Chronicle screen-cache scope, cloud-handoff pressure, worktree pressure, model-provider auth commands/env vars, model/effort task fit, named speed-profile health, response verbosity/reasoning-summary shape, context budget, active AGENTS guidance, custom base/compact prompt overrides, deprecated custom prompts, command-rule files, managed config layers, auth credential storage, the active service tier, approval flow, Goal Mode, memory use/generation, MCP startup health, lifecycle hook load, reusable AGENTS guidance, named profile files, project `AGENTS.md` and local-environment setup/action counts, live helper pressure, background command pressure, concurrency pressure, custom-agent validity, trusted project count, enabled tool surface, skill catalog pressure, subagent depth/thread caps, and a copyable Fix Kit for safe next steps. Destructive deletion actions stay locked until you switch to Hard Mode and enable `Deletes On`. SQLite state changes create backups under the app data directory.

`Smart Optimize` builds a plan from the current scan. `Safe` runs non-destructive cleanup first: move archived transcripts out of active sessions, archive stale thread rows, compact state, prune/checkpoint logs with a backup, clear crash dumps, and clean rebuildable browser caches. `Recover Space` can delete old archived conversations and old Refit backups after the selected age, but only after deletes are explicitly allowed. `Full Pass` can also move older generated-image folders from `~/.codex/generated_images` to `~/.codex/archived_generated_images`; generated images are never deleted by Codex Refit.

## Codex Speed Playbook

Codex Refit checks three kinds of speed pressure:

- Local state: active transcripts, media-heavy appshot/image sessions, archived pointers still sitting in active sessions, SQLite log/WAL size, crash dumps, and rebuildable browser caches.
- Runtime and config: official Doctor findings, CLI/app version drift, Codex launch latency, strict config validation, Goal Mode readiness, Validation Loop readiness, Review Lane readiness, Codex manual freshness, live app-server/helper load, app-server transport/listener health, SSH remote-host readiness, shell startup latency, CLI completion setup, background terminal commands, Codex-managed worktree pressure, default model/reasoning task fit, cloud-handoff Git/GitHub branch readiness, model-provider/env/auth-command health, named speed-profile health, subagent depth/thread fan-out, custom-agent model/instruction/MCP load, MCP/plugin startup surface, shell snapshot and command environment policy, notification flow, history retention, storage paths, telemetry export, status-line visibility, Codex exec automation hygiene, response verbosity/reasoning-summary shape, effective web-search mode and deprecated web-search toggles, network/sandbox mode and proxy policy, scoped permissions profiles, Chronicle temporary screen cache and generated-memory load, current-project PLANS.md/readiness, context/tool-output budget, instruction-stack size/caps, custom instruction/base/compact prompt overrides, command rules, managed requirements/defaults, lifecycle hook load, approval flow, approval/sandbox friction, auto-review mode, memory context settings/storage, auth cache metadata, Fast Mode eligibility, model and reasoning effort, and trusted project entries.
- Workflow context: sampled active-thread task shape, observed turn duration, first-token delay, rate-limit metadata, near-context-window token counts, approval/permission/sandbox stop markers, empty or missing global/project `AGENTS.md`, oversized or capped durable guidance, deprecated custom prompts, missing `.codex` local setup/actions, current-project `codex exec`/GitHub Action patterns, large skill catalogs, overly broad concurrent threads, and task/model mismatch.

Plan Effort is a dedicated Doctor and Speed Check signal. It looks for `plan_mode_reasoning_effort` so ambiguous work can use deeper planning while everyday turns and named speed profiles stay lighter.

The app does not silently rewrite Codex config, inspect auth token contents, print model-provider token values, print app-server WebSocket token paths or token hashes, connect to SSH hosts, print SSH hostnames, print SSH usernames, print SSH key paths, run notification commands, delete or trim `history.jsonl`, move custom storage paths, emit telemetry, print automation prompt text or secret values, print shell config contents while checking CLI completions, print Codex manual contents while checking manual freshness, print Git diffs while checking Review Lane readiness, print guidance contents while checking Validation Loop readiness, or print transcript contents while checking media, task-shape, Goal Mode, turn-telemetry, and approval-friction pressure. It does run no-op shell startup timing checks such as `SHELL -lc ':'`, Codex `--version` launch probes, Git status/shortstat checks, and the official `codex --strict-config --version` validation, then keeps status, timing, and redacted first-line failure metadata only. It surfaces the next move and copyable snippets so you can decide whether to run cleanup, archive finished visual-debugging threads, close idle Codex threads and terminals, inspect background work with `/ps`, stop obsolete background terminal work with `/stop`, compact or split sprawling active threads, check `/status` for context and rate limits, refresh the local Codex manual cache when docs-informed advice gets stale, run `/review` before committing large local diffs, restore useful `status_line` fields, install shell completions with `codex completion`, compare Codex `--version` launch time, run `codex --strict-config --version` before trusting new speed settings, use `/goal` for persistent completion criteria on long work, add a clear test/build/review proof path, pause Chronicle before sensitive or rate-limit-heavy work, enable lightweight unfocused notifications for long work, cap Codex history retention with `history.max_bytes`, keep SQLite/log storage on local unsynced disk, turn off remote telemetry export outside tracing sessions, make throwaway `codex exec` scripts ephemeral and machine-readable, prefer the Codex GitHub Action for GitHub Actions automation, keep app-server WebSocket listeners loopback-only or authenticated, add a concrete SSH Host alias for remote project offload, keep non-interactive shell startup light, push or hand off GitHub-backed work to cloud, cap subagent fan-out, review custom agents, review MCP servers with `/mcp verbose`, fix model-provider env/auth-command setup, trim spawned-command environment policy, review network/sandbox settings, use scoped permissions profiles instead of broad defaults, review context budget and use `/compact`, review active AGENTS guidance, review custom instruction overrides, test command rules with `codex execpolicy check`, review approval flow and approval/sandbox friction with `/permissions`, review memories, review installed skills with `/skills`, migrate deprecated custom prompts into skills, disable stale skills with `[[skills.config]]`, refresh login with `codex doctor`/`codex login`, fix terminal/auth/update/MCP findings from the official Doctor report, create a named `speed.config.toml` profile, add a project playbook, switch model/effort for a small task, check `/fast status`, align CLI/app versions, remove stale trusted paths, or add concise AGENTS guidance. This follows the Codex manual guidance that `codex doctor` can check config/auth/runtime health, `--strict-config` errors when config contains fields the current Codex version does not recognize, Goal Mode gives Codex a persistent objective and completion criteria across longer tasks, `/status` shows context and rate-limit state, `status_line` defaults include model/reasoning and remaining context, `codex completion` can generate shell completion scripts for everyday CLI speed, login details are cached in `auth.json` or an OS credential store, prompts get better when they include goal, context, constraints, and done-when checks, Codex works better when it knows how to test, check, confirm behavior, and review diffs, `/review` can inspect uncommitted changes without touching the working tree, cloud threads clone GitHub-backed repos and can run heavy/parallel work away from the local machine, the Codex App can add SSH hosts from concrete `~/.ssh/config` aliases, pattern-only SSH hosts are ignored, remote SSH project threads run against the remote filesystem and shell, the remote login shell needs `codex` on PATH and authenticated, custom providers can use exactly one auth style such as `env_key`, OpenAI auth, or command-backed auth, provider/profile/notification/telemetry keys in project `.codex/config.toml` are ignored, `gpt-5.5` is best for complex work, `gpt-5.4-mini` is faster for lighter coding, Codex-Spark is a near-instant iteration model when available, appshots and image inputs are stored locally in session history, `history.persistence` defaults to `save-all`, `history.max_bytes` can cap `history.jsonl` by trimming oldest entries, `CODEX_HOME` stores Codex state, `log_dir` explicitly enables the plaintext `codex-tui.log`, `sqlite_home` and `CODEX_SQLITE_HOME` can redirect SQLite-backed state, OpenTelemetry is disabled by default, `otel.exporter = "none"` sends nothing, exporters batch asynchronously and flush on shutdown, `otel.log_user_prompt` defaults to false so user prompts stay redacted, project local environments can define worktree setup scripts and common actions, Codex-managed worktrees live under `$CODEX_HOME/worktrees` and the default managed-worktree keep limit is 15, `codex exec --ephemeral` avoids persisting session rollout files for throwaway automation, `codex exec --json` emits JSONL events, `--output-schema` and `--output-last-message` make script output easier to consume, `--full-auto` is deprecated in favor of explicit sandbox flags, `CODEX_API_KEY` is only supported for `codex exec`, the Codex GitHub Action is the recommended GitHub Actions path, app-server stdio and unix transports are the normal local transports, WebSocket app-server transport is experimental and unsupported, loopback WebSocket listeners are suitable for localhost and SSH-forwarding workflows, and non-loopback WebSocket listeners should use app-server WebSocket auth flags, Fast Mode can accelerate supported models at higher credit use, shell snapshots speed repeated command setup, `shell_environment_policy` controls the env vars passed to spawned commands, workspace-write command network access is off unless `sandbox_workspace_write.network_access = true`, `network_proxy` constrains enabled command network access but does not grant it, permissions profiles can add concrete workspace roots, scoped filesystem rules, and domain-limited network access, Chronicle can recover recent screen context but can consume rate limits quickly and stores generated memories under `memories_extensions/chronicle`, Codex supports TUI notifications and a user-level external `notify` argv command, global `*` allow rules are broad network access, model context contains file reads, tool output, and conversation history, `/compact` frees tokens in long threads, Codex reads global and project `AGENTS.md` guidance before work, closer project guidance wins, empty AGENTS files are skipped, `project_doc_max_bytes` defaults to 32 KiB, `developer_instructions` are injected before AGENTS guidance, `model_instructions_file` can override built-in base instructions, compact prompt overrides can affect long-thread summarization, configured MCP servers launch when a session starts, required MCP servers can fail startup if initialization breaks, Codex uses sandbox mode for what commands can technically do and approval policy for when it must stop before acting, granular approval policy can cover sandbox approvals, execpolicy-rule prompts, MCP prompts, `request_permissions` prompts, and skill-script approvals, Codex scans `rules/` under active config layers at startup, broad command `prefix_rule` patterns can increase approval churn, `codex execpolicy check` tests how rule files apply to a command, managed `requirements.toml` can constrain approval, sandbox, web-search, MCP, features, hooks, and rules, `managed_config.toml` can set startup defaults above local config, automatic approval review can add model calls for eligible interactive approvals, `/ps` shows background terminals, `/stop` stops background terminals, matching hooks from multiple files can all run, turn/tool-scope hooks can add latency around frequent actions, `/hooks` shows loaded hook sources, custom agents live in `~/.codex/agents/` or project `.codex/agents/`, custom agents can override model/effort/MCP/sandbox/skills settings for spawned sessions, each subagent does its own model and tool work, custom prompts are deprecated and only scan top-level Markdown files in `~/.codex/prompts`, skills start with name/description/path metadata before Codex loads full `SKILL.md`, the initial skill list has a documented context budget, `agents.max_threads` defaults to 6, `agents.max_depth` defaults to 1, raising subagent depth can increase token use, latency, and local resource consumption, and memories can inject useful local context into future sessions.

The imagegen concept comp lives at `public/design-comps/codex-refit-interface-concept.png`.
The generated menu bar icon source lives at `public/app-icons/codex-refit-menubar-source.png`, with transparent app assets in `public/app-icons/` and white-only tray-sized PNGs in `electron/`.
